[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast deterministic tests (no heavy search)
    e2e: smoke tests that run search or CLI
    build: fast CI subset (unit + stable e2e)
    slow: large grids or optional matrices
